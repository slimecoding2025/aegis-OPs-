Drop these two files directly into this `public/` folder:

1. `profile.jpg` — your photo. Until it exists, the About section shows a
   styled placeholder avatar automatically (see components/About.jsx).
2. `cv.pdf` — your CV/resume. The hero "Download CV" button already points
   at `/cv.pdf`; it will start working the moment the file is here.
